services:
  backend:
    build: ./backend
    volumes:
      - /home/ubuntu/proCreate/data:/data
    environment:
      DATABASE_PATH: /data/showcase.db
    restart: unless-stopped

  web:
    build: ./frontend
    ports:
      - "8096:80"
    depends_on:
      - backend
    restart: unless-stopped
